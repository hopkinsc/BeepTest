# BeepTest

A tiny browser-based worm game made with plain HTML, CSS, and JavaScript.

## Run locally

Because the game uses `localStorage` for best score, launch it via a local web server:

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000>.
